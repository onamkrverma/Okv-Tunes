import React from "react";

const Navbar = () => {
  return (
    <header className="page-container sticky top-0 left-0 backdrop-blur-lg bg-primary/50 px-10 py-2 border-b">
      search box
    </header>
  );
};

export default Navbar;
